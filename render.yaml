services:
  - type: web
    name: inventory-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn warehouse.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: warehouse.settings
      - key: PYTHON_VERSION
        value: 3.11
